// src/components/ConsentBanner.tsx
import React from 'react';

/**
 * Cloudflare Web Analytics is cookieless and does not require consent.
 * This component is intentionally a no-op. If you later add tools that need consent,
 * reintroduce a banner here with vendor-neutral wording.
 */
export default function ConsentBanner() {
  return null;
}
